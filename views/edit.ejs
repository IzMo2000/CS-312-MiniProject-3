<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Post</title>
        <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css" />
    </head>

    <body class="bg-light">
        <div class="container mt-5">
            <h2 class="mt-5">Edit Post</h2>
            <form action="/edit_post" method="POST" class="mt-3">
                <input type="hidden" name="id" value="<%= locals.postToEdit.id %>">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" name="title" class="form-control mb-3" value="<%= locals.postToEdit.title %>">
                </div>
                <div class="form-group">
                    <label for="author">Author</label>
                    <input type="text" name="author" class="form-control mb-3" value="<%= locals.postToEdit.author %>">
                </div>
                <div class="form-group">
                    <label for="content">Content</label>
                    <textarea name="content" class="form-control mb-3"><%= locals.postToEdit.content %></textarea>
                </div>
                <input type="submit" class="btn-primary" value="Edit Post">
            </form>
        </div>
    </body>
</html>